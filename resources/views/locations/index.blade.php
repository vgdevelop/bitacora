@extends('layouts.app') @section('title','Instalaciones') @section('content')<div class="page-head"><div><span class="eyebrow">MAPA OPERATIVO</span><h1>Salas y contenedores</h1><p>Equipamiento instalado e historial de intervenciones por ubicación.</p></div></div><section class="location-grid">@forelse($locations as $location)<a class="card location-card" href="{{ route('locations.show',$location) }}"><div><span class="location-code">{{ $location->code }}</span><span class="pill">{{ $location->type_label }}</span></div><h2>{{ $location->name }}</h2><p>{{ $location->zone ?: 'Sin zona asignada' }}</p><div class="location-stats"><span><strong>{{ $location->assets_count }}</strong> equipos</span><span><strong>{{ $location->work_logs_count }}</strong> trabajos</span></div><ul>@foreach($location->assets->take(3) as $asset)<li><i class="asset-dot asset-{{ $asset->status }}"></i>{{ $asset->name }} <small>{{ $asset->status_label }}</small></li>@endforeach</ul></a>@empty<div class="empty">No hay instalaciones cargadas.</div>@endforelse</section>@endsection
