@extends('layouts.app') @section('title','Editar '.$workLog->number) @section('content')<div class="page-head"><div><span class="eyebrow">{{ $workLog->number }}</span><h1>Editar registro</h1><p>Corrección supervisada de datos, estado y horarios.</p></div></div><form class="card log-form" method="post" action="{{ route('admin.logbook.update',$workLog) }}">@csrf @method('PUT') @include('logbook.form')</form>@endsection
