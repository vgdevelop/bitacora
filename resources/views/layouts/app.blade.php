<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><meta name="theme-color" content="#153b2f"><title>@yield('title','Bitácora') · Planta Norte</title><link rel="stylesheet" href="{{ asset('css/bitacora.css') }}"><link rel="stylesheet" href="{{ asset('css/operations.css') }}"></head><body>
@auth<header class="topbar"><div class="shell nav"><a class="brand" href="{{ route('logbook.index') }}"><span class="brand-mark">PN</span><span><strong>PLANTA NORTE</strong><small>Bitácora industrial</small></span></a><nav><a href="{{ route('logbook.index') }}">Bitácora</a><a href="{{ route('locations.index') }}">Instalaciones</a>@if(auth()->user()->isSupervisor())<a href="{{ route('admin.plant.index') }}">Administración</a><a href="{{ route('admin.qr.index') }}">Códigos QR</a><a class="new-link" href="{{ route('admin.logbook.create') }}">+ Registro manual</a>@endif</nav><div class="user"><span>{{ auth()->user()->name }}</span><form method="post" action="{{ route('logout') }}">@csrf<button>Salir</button></form></div></div></header>@else<header class="topbar"><div class="shell nav"><span class="brand"><span class="brand-mark">PN</span><span><strong>PLANTA NORTE</strong><small>Bitácora industrial</small></span></span></div></header>@endauth
@if(session('success'))<div class="shell notice">{{ session('success') }}</div>@endif @if($errors->any())<div class="shell notice error-notice"><strong>Revisa los datos:</strong><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<main class="shell">@yield('content')</main><footer><div class="shell">Sistema interno de trazabilidad operativa · {{ date('Y') }}</div></footer></body></html>
